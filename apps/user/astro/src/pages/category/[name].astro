---
import BaseLayout from '@/layouts/BaseLayout.astro';
const { name } = Astro.params;

export function getStaticPaths() {
  return [];
}
---
<BaseLayout title={`${name}`}>
  <h1>카테고리별 페이지</h1>
  <p>route: /category/[name]</p>
  <p>category: {name}</p>
</BaseLayout>
