---
import BaseLayout from '@/layouts/BaseLayout.astro';
const { name } = Astro.params;

export function getStaticPaths() {
  return [];
}
---
<BaseLayout title={`${name} 스냅`}>
  <h1>학교별 페이지</h1>
  <p>route: /school/[name]</p>
  <p>school: {name}</p>
</BaseLayout>
